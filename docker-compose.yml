version: '3.8'
services:
  backend:
    build: 
      context: ./backend
    ports:
      - "8080:8080"
    restart: unless-stopped
    
  frontend:
    image: nginx:alpine
    ports:
      - "8000:80"
    volumes:
      - ./frontend:/usr/share/nginx/html:ro
    restart: unless-stopped
